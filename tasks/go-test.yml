platform: linux

image_resource:
  type: registry-image
  source: { repository: golang }

inputs:
- name: repo

params:
  MODULE:

run:
  path: sh
  args:
  - -ce
  - |
    cd "repo/${MODULE}"
    go test -v ./...
